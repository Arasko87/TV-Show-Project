/*
      let episodeAndSeason = document.createElement("h3");
      divForBox.appendChild(h3);
      episodeAndSeason.className = "codeOfEpisodes";
      episodeAndSeason.innerText = episode.season;   //`S${addZeroPrefix(episode.season)}E${addZeroPrefix(episode.number)}`;
     */