

//=================================================================



/*
        episodeList.forEach(function (contents) {
  let allContent = document.createElement("div");
      allContent.className = "allContent";
      rootElem.appendChild(allContent);
      }

      ==========================
//the link to the Maze site.
  var a = document.createElement('a');  
  var link = document.createTextNode(`Maze Original website`); 
         a.appendChild(link);  
           a.href = "https://www.tvmaze.com/";  
           document.body.appendChild(a);  
//==============

// Table contents.

  let table = document.createElement("table");
  rootElem.appendChild(table);
  table.className = "tableContent";
  //let row = table.insertRow();
  for (let i = 0; i< episodeList.length; i++){
    let row = table.insertRow();
    row.className = "tableRow";
      cell1 = row.insertCell();
      cell1.innerText = episodeList[i].name.concat(innerText =` - S0${episodeList[i].season}E0${episodeList[i].number}`);
    let img1 = row.insertCell();
    let img = document.createElement("img");
          img.src = episodeList[i].image.medium;
          img1.appendChild(img);
        row.insertCell().innerHTML = episodeList[i].summary;

  }

}
}}
*/


